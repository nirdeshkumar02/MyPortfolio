import Head from "next/head";

const Title = (props) => (
  <div>
    <Head>
      <title> Portfolio - Nirdesh Kumar Saini</title>
    </Head>
    {/* the rest of the JSX */}
  </div>
);

export default Title;
